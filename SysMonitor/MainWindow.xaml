<Window x:Class="SystemMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System Monitor" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- System Information Panel -->
        <GroupBox Header="System Information" Grid.Row="0" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- CPU -->
                <TextBlock Text="CPU" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ProgressBar x:Name="CpuProgressBar" Grid.Row="0" Grid.Column="1" Height="18" Minimum="0" Maximum="100"/>
                <TextBlock x:Name="CpuLabel" Grid.Row="0" Grid.Column="3" VerticalAlignment="Center" Margin="6,0,0,0" Text="CPU: 0%"/>

                <!-- Memory -->
                <TextBlock Text="Memory" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,4,6,0"/>
                <ProgressBar x:Name="MemoryProgressBar" Grid.Row="1" Grid.Column="1" Height="18" Margin="0,4,0,0" Minimum="0" Maximum="100"/>
                <TextBlock x:Name="MemoryLabel" Grid.Row="1" Grid.Column="3" VerticalAlignment="Center" Margin="6,4,0,0" Text="Memory: 0%"/>

                <!-- Disk -->
                <TextBlock Text="Disk" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,4,6,0"/>
                <ProgressBar x:Name="DiskProgressBar" Grid.Row="2" Grid.Column="1" Height="18" Margin="0,4,0,0" Minimum="0" Maximum="100"/>
                <TextBlock x:Name="DiskLabel" Grid.Row="2" Grid.Column="3" VerticalAlignment="Center" Margin="6,4,0,0" Text="Disk: 0%"/>
            </Grid>
        </GroupBox>

        <!-- Process list -->
        <DataGrid x:Name="ProcessDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Process Name" Binding="{Binding ProcessName}" Width="*"/>
                <DataGridTextColumn Header="Memory (MB)" Binding="{Binding MemoryMb}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
